/**
 * Data access goes here. No persistence is wired up yet — add a database
 * dependency (e.g. spring-boot-starter-data-jpa + a driver) and Spring Data
 * repository interfaces when ready to persist real data.
 */
package com.shelf.backend.repository;
